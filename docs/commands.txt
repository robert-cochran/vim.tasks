COMMANDS

    CUSTOM-COMMANDS
    DoneTest
    DoneTask
    NewTask
    MoveWrite
    MoveAppend
    .... etc., there are more but thats the idea
   
    SELCTING
    vip         visually select everything inside the paragraph youre in
    

    INDENTING
    .           in normal mode, this will repeat last command
    >           indents a selection by tab
    <           deindents
    >>          indenting
    <<          deindenting 

    FOLD
    vip         visually select everything inside the paragraph youre in
    zf          create fold
    zo          open fold
    zc          close fold
    zC          close all folds
    zO          open all folds
    ctrl+w T    opens buffer into new tab (helpful when opening help documents that splits the term)
    ctrl+w o    closes window
    ctrl+w c    closes window (yea i dunno either, one of them is probably wrong)
